import React from 'react';

const Recipe = ({ recipe }) => { 
  const { id, title, time, cook, price, difficulty }
} = recipe;
console.log(recipes);

return ( 
  <div className='recipe'> 

  </div>
)
}